{
  "name": "yobs",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.2.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "start": "node apps/api/dist/main.js"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}